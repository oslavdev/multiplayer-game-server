generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id         String  @id
  username   String
  email      String
  password   String
  activated  Boolean
  admin      Boolean
  level      Int
  moneyCents Int
}
